# 🐳 Docker 命令完整参考指南

### 🌍 语言 / Languages

[🇬🇧 English](./README.md) | [🇫🇷 Français](./README-FR.md) | [🇪🇸 Español](./README-ES.md) | [🇮🇹 Italiano](./README-IT.md) | [🇩🇪 Deutsch](./README-DE.md) | [🇵🇹 Português](./README-PT.md) | [🇨🇳 中文](./README-ZH.md)

---

## 🔧 1. 安装与服务

*守护进程管理和系统状态。*

| 命令 | 描述 |
| --- | --- |
| `sudo pacman -S docker` | 安装 Docker（Arch Linux）。 |
| `sudo apt install docker.io` | 安装 Docker（Debian/Ubuntu/Pop!_OS）。 |
| `sudo dnf install docker` | 安装 Docker（Fedora/RHEL）。 |
| `docker version` | 显示已安装的 Docker 版本。 |
| `systemctl status docker` | 检查 Docker 服务状态。 |
| `systemctl start docker` | 启动 Docker 服务。 |
| `systemctl enable docker` | 设置 Docker 开机自启。 |

---

## 💿 2. Docker 镜像

*用于创建容器的只读模板。*

| 命令 | 描述 |
| --- | --- |
| `docker images` | 列出所有本地镜像。 |
| `docker pull <镜像>` | 从 Docker Hub 下载镜像。 |
| `docker rmi <镜像>` | 删除本地镜像。 |
| `docker build -t <名称> .` | 通过 Dockerfile 构建镜像。 |
| `docker image inspect <名称>` | 显示镜像详细信息。 |
| `docker history <镜像>` | 显示镜像的历史记录（层）。 |

---

## 📦 3. 容器

*镜像的可运行实例。*

### ▶️ 运行

* `docker run <镜像>` ：运行一个基本容器。
* `docker run -it --name <名称> <镜像>` ：以交互模式运行并附带终端。
* `docker run -d --name <名称> <镜像>` ：在后台运行（分离模式）。
* `docker run -p 8081:80 <镜像>` ：端口映射（主机:容器）。
* `docker run --restart always <镜像>` ：确保容器自动重启。

### 📋 管理

| 命令 | 描述 |
| --- | --- |
| `docker ps` | 列出正在运行的容器。 |
| `docker ps -a` | 列出所有容器（包括已停止的）。 |
| `docker stop <名称>` | 停止一个正在运行的容器。 |
| `docker start <名称>` | 启动一个已停止的容器。 |
| `docker rm <名称>` | 删除一个容器。 |
| `docker rename <旧名称> <新名称>` | 重命名现有容器。 |

### 🔍 调试与监控

* `docker stats` ：**实时流**显示容器资源使用情况（CPU/内存）。
* `docker top <名称>` ：显示容器的运行进程。
* `docker logs -f <名称>` ：实时跟踪日志。
* `docker exec -it <名称> bash` ：在容器内打开终端。
* `docker inspect <名称>` ：查看技术详情（IP、配置等）。

### 📂 文件传输

* `docker cp <主机路径> <容器>:<路径>` ：从主机复制文件到容器。
* `docker cp <容器>:<路径> <主机路径>` ：从容器复制文件到主机。

---

## 💾 4. 数据卷与存储

*跨容器重启的数据持久化。*

* `docker volume create <名称>` ：创建命名数据卷。
* `docker volume ls` ：列出所有数据卷。
* `docker volume rm <名称>` ：删除指定数据卷。
* **挂载：**
* `docker run -v 卷名称:/data <镜像>` ：使用命名数据卷。
* `docker run -v $(pwd):/app <镜像>` ：绑定挂载（当前本地目录）。

---

## 🌐 5. 网络

*管理容器之间的通信。*

* `docker network ls` ：列出网络。
* `docker network create <名称>` ：创建新网络。
* `docker network connect <网络> <容器>` ：将容器连接到网络。
* `docker network disconnect <网络> <容器>` ：将容器从网络断开。

---

## 🧩 6. Docker Compose

*定义和运行多容器应用。*

| 命令 | 描述 |
| --- | --- |
| `docker-compose up -d` | 在后台启动服务。 |
| `docker-compose down` | 停止并移除资源（容器、网络）。 |
| `docker-compose ps` | 列出服务状态。 |
| `docker-compose logs -f` | 跟踪所有服务日志。 |
| `docker-compose build --no-cache` | 从头重新构建镜像。 |

---

## ☁️ 7. Docker Hub 与镜像仓库

*分享和存储你的镜像。*

1. `docker login` ：登录镜像仓库。
2. `docker tag <本地镜像> <用户>/<仓库>:<标签>` ：为上传准备镜像。
3. `docker push <用户>/<仓库>:<标签>` ：推送镜像到 Hub。

---

## 🏗️ 8. 编排（Swarm 与 Stack）

*用于服务器集群和生产部署。*

* `docker swarm init` ：初始化 Swarm 集群。
* `docker node ls` ：列出集群节点。
* `docker service create --name <名称> --replicas 3 <镜像>` ：创建服务。
* `docker service scale <名称>=5` ：将服务扩展到 5 个副本。
* `docker stack deploy -c docker-compose.yml <栈名称>` ：部署一个栈。

---

## 🧹 9. 系统与维护

*清理和诊断。*

* `docker system df` ：查看 Docker 磁盘使用情况。
* `docker system prune` ：移除未使用的数据（已停止的容器、未使用的网络）。
* `docker system prune -a` ：**深度清理**（移除所有未使用的镜像和数据）。
* `docker image prune` ：仅移除悬空镜像（无标签）。
